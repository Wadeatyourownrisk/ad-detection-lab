YYYY-MM-DD — [Short title]
Phase: [1-Build / 2-Weaken / 3-Attack / 4-Detect / 5-LLM-Triage / 6-Synthetic Users / Other] Time spent: [~Xh] Status: [Complete / In progress / Blocked / Abandoned]

Goal
What I set out to do and why it matters for the lab.

What I did
Concrete actions, tools used, commands run, configs changed. Bullet points are fine.

What went wrong
Errors, dead ends, misconceptions. Be specific - "DNS broke" is useless, "clients couldn't resolve DC because forwarders were pointed at 8.8.8.8 instead of the DC itself" is gold.

Resolution
How I fixed it, or current workaround if unresolved.

Takeaway
The one or two things I actually learned. This is the most important section — if you skip everything else, write this.

Artifacts
Files committed: path/to/file
Screenshots: path/to/img
Related rules/configs:
Next
What this unblocks or what I want to try next. Feed anything bigger into BACKLOG.md.

Tags
#ad #networking #wazuh #attack #detection #llm (pick what applies — makes grep easier later)
